# 拉屎打卡小程序 - 开发计划

## 功能概览
1. **首页**：大圆形按钮 → 点击后填写今日形状 + 份量 → 打卡
2. **分享**：生成今日打卡图片，可分享给微信好友
3. **日历页**：显示本月打卡情况（分享后或不分享后自动进入）

---

## 分步计划（奥卡姆剃刀：每次只做必要的一步）

### 阶段一：骨架跑通
- **Step 1**：最简小程序结构（app + 一个首页），能在开发者工具里跑起来
- **Step 2**：首页只放一个大圆形按钮，点击有反馈（如弹窗），确认交互正常

### 阶段二：打卡数据
- **Step 3**：点击按钮后弹出/跳转到「填写页」：选择形状（如 Bristol 类型或简化版）、份量（少/中/多）
- **Step 4**：点击「打卡」把一条记录存到本地（wx.setStorageSync），数据结构：日期 + 形状 + 份量
- **Step 5**：打卡成功后跳转到「结果页」，显示今日记录（先简单文字即可）

### 阶段三：分享图片
- **Step 6**：结果页用 canvas 绘制「今日拉屎」卡片图（日期、形状、份量等）
- **Step 7**：把 canvas 转成图片，接入微信「分享给好友」或「保存图片」能力

### 阶段四：日历
- **Step 8**：增加日历页，展示当月日历，有打卡的日期做标记（小圆点/高亮）
- **Step 9**：从结果页「返回/不分享」后自动跳转到日历页；首页可进日历（如顶部或按钮）

### 阶段五：打磨（可选）
- **Step 10**：样式美化、动画、空状态提示等

---

## 技术要点（你会用到的）
- **页面**：pages/index（首页）、pages/record（填写+打卡）、pages/result（结果+分享图）、pages/calendar（日历）
- **数据**：本地存储 key 如 `shit_records`，存数组 `[{ date, shape, amount }, ...]`
- **分享图**：canvas 画好 → `wx.canvasToTempFilePath` 得到临时路径 → 用于分享或保存

---

我们从 **Step 1** 开始：先建好最简小程序骨架，你能在微信开发者工具里打开并看到首页。
